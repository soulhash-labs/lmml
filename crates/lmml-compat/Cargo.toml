[package]
name = "lmml-compat"
version = "0.1.0"
edition = "2021"
description = "llama.cpp server compatibility layer for lmml"

[dependencies]
thiserror = "2"
tokio = { version = "1", features = ["process", "rt", "macros"] }
